name = "port"# ⚠️ CRITICAL: This is a CLOUDFLARE PAGES project, NOT a Worker!
# 
# DO NOT run: wrangler deploy
# DO run instead:
#   1. GitHub integration: Connect repo to Cloudflare Dashboard
#   2. CLI: npx wrangler pages deploy dist
#
# This is a Vite + React SPA that builds to static files (dist/)

compatibility_date = "2026-06-08"
pages_build_output_dir = "dist"

[assets]
directory = "./dist"
